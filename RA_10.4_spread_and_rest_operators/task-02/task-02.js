function sumAll(x, y, ...numbers) {
  let total = x + y;

  // Todo: Add the rest of the numbers to the `total` variable

  return total;
}

if (typeof module !== "undefined") {
  module.exports = { sumAll };
}
