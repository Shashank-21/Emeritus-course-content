function combineDishes(dishes1, dishes2) {
  // Todo: Use the spread operator to combine all elements of dishes1 and dishes2 array into the dishes array
  const dishes = [];

  return dishes;
}

if (typeof module !== "undefined") {
  module.exports = { combineDishes };
}
